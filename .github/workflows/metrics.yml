name: Run Daily Metrics

on:
  workflow_call:
    secrets:
      METRICS_TOKEN:
        required: true
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run_habits:
    name: Run Habits
    uses: ./.github/workflows/habits.yml
    secrets: inherit
